# Campaign log

One row per attempt. `Result`: clean-pass / needed-fix / blocked / failed. `Skill update`: what (if anything) got folded back into a CARD or core/mobile-ux-primitives.

| # | Task | App | Result | Notes | Skill update |
|---|------|-----|--------|-------|---------------|
| 3 | Find Android version | Settings | clean-pass | Found under À propos du téléphone > Version d'Android = 15. Dense-list prediction held. | none needed |
| 7 | Find free storage | Settings | clean-pass | Visible directly in the main Settings list ("Xx Go disponible"), no drill-down needed. | none needed |
| 1 | Battery percentage | Settings | clean-pass | Battery % visible directly in main Settings list, updates live (49%→47% observed across the session). | none needed |
| 51-installs | Install Reddit + Wikipedia | Play Store | needed-fix (workflow, not a skill gap) | Both installs sat in "En attente" for 5-10+ min, cycling between pending/Play-Protect-validated/pending. Not a CARD-level fact, but an operational note for anyone running autotap-style tasks against this cloud device: budget real wall-clock time for installs, don't treat a long pending state as stuck. | logged to memory (see below), not core/ — this is device/infra-specific, not a UX convention |
| n/a | Play Store search field | Play Store | needed-fix | Reproduced the exact same search-focus gotcha found earlier in Settings: first tap+type silently didn't land, second tap fixed it. Confirms this is a general Android search-field pattern, not a Settings-app quirk. | already documented in navigation-patterns.md from the earlier session — this occurrence reinforces it, no new edit needed |
| n/a | Triple-click to select-all in a search field | Play Store | failed (my approach, not the phone's fault) | Tried a desktop-style triple-click-to-select-all before retyping; didn't select/clear anything on this touch UI. Fixed by going back and starting a fresh search instead of editing in place. | new note added to campaign methodology below — treat touch text fields as tap-to-focus + type, not desktop text-editing gestures |
| 16 | List top-level folders | Files | clean-pass | Hamburger opened a global drawer (Récents/Images/Vidéos/Fichiers audio/Documents/Téléchargements/device root) exactly as predicted; device root showed standard Android folders (Alarms, Android, DCIM, Download, Music, Pictures, etc.) in a grid. | none needed |
| 18 | Search in Files | Files | clean-pass | Search icon → text field worked on the *first* tap this time (no refocus gotcha) — worth noting the gotcha isn't universal, it's intermittent/context-dependent. | none needed |
| 28 | Open Timer, note default duration options | Clock | clean-pass | First task run via Task Runner (not manual Chrome driving) — see [runs/1-timer-app.md](runs/1-timer-app.md) for full 27-event trace. `home` → `open_app('Clock')` → click "Minuteur" tab → correctly reported no presets, keypad-only entry, in 3 turns, no correction needed. | none needed |
| 16 (Task Runner) | List top-level folders | Files | needed-fix (self-healed) | Second Task Runner task — see [runs/2-files-folders.md](runs/2-files-folders.md). `open_app('Files')` landed on what the agent read as a search/empty state, not the root grid; it tapped "Retour" (back) and recovered to the correct root listing itself, one turn later. Folder set matches the manual session's earlier result. Flagged discrepancy: manual session reached root via hamburger-drawer, this run reached it via back-from-search-state — same destination, different path, not yet clear if that's cold-start non-determinism. | none yet — logged as an open question for the repeat round, not promoted |
| 2 | Dark theme on then off | Settings | **failed** | See [runs/3-dark-theme.md](runs/3-dark-theme.md), 62-event trace. Hit the known search-field focus race (typed "Thème sombre", zero results, tool falsely reported success), gave up on search after one Enter retry, fell back to 3 blind scrolls, never found "Affichage" (Display), reported failure at 29s. Most useful failure of the campaign so far — a documented gotcha causing a real task loss because the agent didn't retry the documented fix. | **yes** — strengthened navigation-patterns.md's search-field note with an explicit "re-tap and retype before trusting zero results" rule; also flagged the `type()` tool's false-positive success report as a product-layer issue, not a skill-content fix |
| 4 | Wi-Fi vs mobile data status | Settings | clean-pass | See [runs/4-wifi-status.md](runs/4-wifi-status.md). Same search field that silently failed in task #2 worked fine here on the identical `type()` call — confirms the gotcha is intermittent, not deterministic, matching the earlier memory note. Self-corrected scroll direction on its own when the first swipe didn't surface the target. | none needed — reinforces existing notes, no new edit |
| 5 | Brightness control: slider or stepped | Settings | needed-fix (self-healed) | See [runs/5-brightness-control.md](runs/5-brightness-control.md), 70-event trace. Task started on a leftover screen from task #4 ("Réseau et Internet"), not a fresh Settings root; agent explored the wrong section for a while, then recovered via home+relaunch (plus one more transient wrong screen) before succeeding — found "Affichage" instantly once actually at Settings root, control is a SeekBar/slider. Also strongly suggests task #2's failure was partly stale-starting-state, not purely the search-field race. | **yes** — new "Recovering from a confusing or stuck state" note in navigation-patterns.md (go home + relaunch, then re-verify, after 2+ unproductive actions); also logged (not fixed) that mobilerun's own tooling does read real resource-ids, contradicting an assumption in eBay's CARD.md |
| 11 | About phone model name | Settings | clean-pass | See [runs/6-about-phone-model.md](runs/6-about-phone-model.md). Fastest task yet (10s, 26 events) — one scroll, model name was already in the row's summary text. | none needed |
| 14 | Bluetooth on/off | Settings | needed-fix (self-healed) | See [runs/7-bluetooth-status.md](runs/7-bluetooth-status.md), 62-event trace. Agent called `type()` with missing args (its own bug), got a clear tool error, self-corrected next turn. Search field returned nothing again (3rd time this campaign) — abandoned search, scrolled up, found "Appareils connectés" with Bluetooth status in its summary. | none needed — reinforces existing search-fallback guidance |
| 21 | Calendar default view (day/week/month/agenda) | Calendar | **completed but wrong answer** | See [runs/8-calendar-default-view.md](runs/8-calendar-default-view.md). Self-healed a Play-Services interstitial on first open, but then answered "month view" based only on a header spinner reading "juillet 2026" — the actual on-screen grid was an hourly day/week view, contradicting its own answer. First case this campaign where the *result* itself was wrong, not just the path to it. | **yes** — new "Don't infer view/state from a header label alone" rule in content-and-feeds.md; also flagged as a methodology point (spot-check results against screenshots, not just trust `Result` text) |
